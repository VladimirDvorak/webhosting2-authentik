# Authentik Database Configuration
AUTHENTIK_DB_NAME=authentik
AUTHENTIK_DB_USER=authentik
AUTHENTIK_DB_PASSWORD=change-me-secure-password

# Authentik Secret Key (generate a secure random string)
AUTHENTIK_SECRET_KEY=change-me-secure-secret-key

# Authentik Image Configuration (optional, uses defaults if not set)
# AUTHENTIK_IMAGE=ghcr.io/goauthentik/server
# AUTHENTIK_TAG=2025.10.2

# Port Configuration
AUTHENTIK_PORT_HTTP=9000
AUTHENTIK_PORT_HTTPS=9443
